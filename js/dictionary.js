'use strict';

window.Dictionary = new Array (

                function randomWord(){
                const word = Dictionary[ Math.floor( Math.random() * ( Dictionary.length - 1 ) ) ] ;

                return word ;
              }

)
